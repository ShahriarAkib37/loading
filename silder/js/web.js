$(function(){

// silder //

$('.silderBox').slick({
    autoplay: true,
    autoplaySpeed: 1000,
    infinite: true,
     speed: 500,
     fade: true,
     prevArrow:(`.left`),
     nextArrow:(`.right`),
  });

// silder // 

})

// // audio//
// window.addEventListener(`click`,() => {
//     document.getElementById(`#autoplay`)play();
// })
